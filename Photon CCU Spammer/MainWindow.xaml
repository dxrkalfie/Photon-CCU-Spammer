<Window x:Class="Photon_CCU_Spammer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Photon CCU Simulator" Height="520" Width="700"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen">

    <Border Background="#FF2A2A40" CornerRadius="15" Padding="0" BorderBrush="#FF4CAF50" BorderThickness="2">
        <Grid>
            <!-- Custom Title Bar -->
            <Border Background="#FF1E1E2D" Height="35" CornerRadius="15,15,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown" Margin="0,0,0,481">
                <DockPanel>
                    <TextBlock Text="Photon CCU Simulator" Foreground="White" VerticalAlignment="Center" Margin="10,0,0,0" FontWeight="Bold"/>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                        <Button Content="—" Width="35" Height="30" Click="MinimizeButton_Click"
                                Background="Transparent" Foreground="White" BorderThickness="0" FontWeight="Bold"/>
                        <Button Content="X" Width="35" Height="30" Click="CloseButton_Click"
                                Background="#FFF44336" Foreground="White" BorderThickness="0" FontWeight="Bold"/>
                    </StackPanel>
                </DockPanel>
            </Border>

            <!-- Main Content -->
            <StackPanel Margin="20,45,20,20">
                <!-- App ID -->
                <TextBlock Text="Realtime/PUN App ID:" Foreground="White" FontWeight="Bold"/>
                <TextBox Name="RealtimeAppIdBox" Width="450" Height="30" Margin="0,5,0,10"
                         Background="#FF3A3A55" Foreground="White" BorderThickness="0" Padding="5" FontSize="14"/>

                <!-- Spawn Interval -->
                <TextBlock Text="Spawn Interval (ms):" Foreground="White" FontWeight="Bold"/>
                <TextBox Name="SpawnIntervalBox" Width="150" Height="30" Margin="0,5,0,10"
                         Background="#FF3A3A55" Foreground="White" BorderThickness="0" Padding="5" FontSize="14" Text="500"/>

                <!-- Buttons -->
                <StackPanel Orientation="Horizontal" Margin="0,10,0,10" HorizontalAlignment="Left">
                    <Button Name="StartButton" Content="Start Simulation" Width="160" Height="35" Margin="0,0,10,0"
                            Background="#FF4CAF50" Foreground="White" FontWeight="Bold" BorderThickness="0"
                            Cursor="Hand" Click="StartButton_Click"/>
                    <Button Name="StopButton" Content="Stop Simulation" Width="160" Height="35"
                            Background="#FFF44336" Foreground="White" FontWeight="Bold" BorderThickness="0"
                            Cursor="Hand" Click="StopButton_Click"/>
                </StackPanel>

                <!-- CCU -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,10,0,10">
                    <TextBlock Text="Current Connected Clients:" Foreground="White" FontWeight="Bold" FontSize="14"/>
                    <TextBlock Name="CCULabel" Text="0" Foreground="#FF4CAF50" FontWeight="Bold" FontSize="16" Margin="10,0,0,0"/>
                </StackPanel>

                <!-- Logs -->
                <TextBlock Text="Logs:" Foreground="White" FontWeight="Bold" FontSize="14" Margin="0,10,0,5"/>
                <Border Background="#FF3A3A55" CornerRadius="10" Padding="5" Height="250">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ListBox Name="LogBox" Background="Transparent" BorderThickness="0" Foreground="White"/>
                    </ScrollViewer>
                </Border>
            </StackPanel>
        </Grid>
    </Border>
</Window>
